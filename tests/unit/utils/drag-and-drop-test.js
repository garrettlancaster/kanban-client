import dragAndDrop from '../../../utils/drag-and-drop';
import { module, test } from 'qunit';

module('Unit | Utility | drag and drop');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = dragAndDrop();
  assert.ok(result);
});
